<!-- TODO write magento alternatives-->
Direct use of $_SERVER Superglobal detected.
$_SERVER, $_POST, $_REQUEST, $_GET, $_FILES
base_encode, strlen, unlink, curl, baseinfo, 
file_get_contents, file_exists
```sh

```